// constants/features.ts
// Feature flags to enable/disable features

/**
 * PROFILE FEATURES FLAG
 * 
 * Set to false to hide all profile-related features (tab, followers, following, highlights, etc.)
 * All profile code remains intact - just hidden from UI
 * 
 * To re-enable: Set to true and restart the app
 */
export const PROFILE_FEATURES_ENABLED = false;

